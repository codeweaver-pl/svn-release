'use strict';

var assert = require("assert"),
  svnr = require("../lib/svn-release");

describe('', function () {
  describe('', function () {
    it('', function () {
      assert.equal(true, true, "msg");
    });
  });
});